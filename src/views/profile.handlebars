<main class="d-flex w-100 flex-grow-1">
  <section class="d-flex flex-column align-items-center w-100">
    <h1 class="bg-secondary text-center text-white w-100 mb-3">PROFILE</h1>
    <section class="d-flex flex-column justify-content-center align-items-center w-25 m-3">
      <img style="width: 360px; height: 360px" class="m-2 object-fit-cover" src="{{profile.photo}}" />
      <h3 class="m-2 p-2 text-center">{{profile.email}}</h3>
    </section>
  </section>
</main>